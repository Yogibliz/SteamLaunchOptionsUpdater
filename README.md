# I set this up in a venv to run. Do it like this to make sure it works:

- python3 -m venv venv

- ./venv/bin/pip install vdf

## Don't forget to update NEW_LAUNCH_OPTIONS and LIBRARY_PATH in run_update.sh before running!

## Run with the bash script:

./run_update.sh
